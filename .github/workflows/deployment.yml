# on:
#   push:
#     branches: [main]

# permissions:
#   contents: read
#   id-token: write

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     outputs:
#       image: ${{ steps.image.outputs.uri }}
#     steps:
#       - uses: actions/checkout@v4
#       - uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}
#       - id: ecr
#         uses: aws-actions/amazon-ecr-login@v2
#       - id: image
#         env:
#           REGISTRY: ${{ steps.ecr.outputs.registry }}
#         run: echo "uri=$REGISTRY/$ECR_REPOSITORY:$GITHUB_SHA" >> "$GITHUB_OUTPUT"
#       - run: docker build --tag "${{ steps.image.outputs.uri }}" .
#       - name: Container scan
#         uses: aquasecurity/trivy-action@0.28.0
#         with:
#           image-ref: ${{ steps.image.outputs.uri }}
#           severity: HIGH,CRITICAL
#           exit-code: "1"
#           ignore-unfixed: true
#       - run: docker push "${{ steps.image.outputs.uri }}"

#   staging:
#     needs: build
#     runs-on: ubuntu-latest
#     environment: staging
#     steps:
#       - uses: actions/checkout@v4
#       - uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#   production:
#     needs: [build, staging]
#     runs-on: ubuntu-latest
#     environment: production # Configure required reviewers for this environment in GitHub.
#     steps:
#       - uses: actions/checkout@v4
#       - uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#   notify-failure:
#     if: always() && failure()
#     needs: [build, staging, production]
#     runs-on: ubuntu-latest
#     steps:
#       - name: Notify Slack
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#         run: |
#           curl --fail-with-body --header 'Content-type:application/json' \
#             --data "{\"text\":\"Deployment failed: $GITHUB_SERVER_URL/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID\"}" \
#             "$SLACK_WEBHOOK_URL"
